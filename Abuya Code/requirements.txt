# Runtime dependencies for the BraTS-PEDs 3D U-Net project.
# Versions are intended for Python 3.12 / Windows.

# Model and training
torch>=2.5.1
torchvision>=0.20.1
tqdm==4.70.0

# Volume loading and array maths
numpy==2.4.4
scipy==1.17.1
nibabel==5.4.2

# 2D slice views
matplotlib==3.10.8
ipywidgets==8.1.8

# 3D surface view
plotly==6.8.0
scikit-image==0.26.0

# Required by plotly to render figures inside Jupyter
nbformat==5.11.0

# Desktop interface and MedSAM2 runtime
PySide6>=6.8.0
hydra-core>=1.3.2
iopath>=0.1.10
pillow>=10.4.0
SimpleITK>=2.4.0

# Optional: static PNG export via fig.write_image(). Needs a local Chrome,
# which it downloads on first use (`kaleido_get_chrome`).
# kaleido==1.3.0
